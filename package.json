{
  "name": "dasu",
  "version": "0.0.20",
  "description": "Consistent xhr API for both client and server (http.request)",
  "main": "dist/bundle.min.js",
  "scripts": {
    "build": "rollup -f umd dasu.js > dist/bundle.js",
    "uglify": "uglifyjs dist/bundle.js -cmo dist/bundle.min.js",
    "prepublish": "npm run lint && npm run build && npm test",
    "lint": "spacestandard --fix dasu.js",
    "test": "npm run lint && npm run build && npm run uglify && tape test/*test*.js | tap-spec"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/talmobi/dasu"
  },
  "author": "talmobi <talmo.christian@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/talmobi/dasu/issues"
  },
  "devDependencies": {
    "body-parser": "^1.16.0",
    "express": "^4.14.0",
    "rollup": "^0.33.1",
    "spacestandard": "0.0.6",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0",
    "uglify-js": "^2.7.5"
  }
}
